import shuffle from "lodash/shuffle"
import * as event from "./event/event"
import * as geometry from "./geometry/geometry"

let Richang = {
    shuffle,
    event,
    geometry
}
export { Richang, shuffle, event, geometry }
